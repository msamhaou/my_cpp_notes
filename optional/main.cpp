#include <iostream>
#include <otional>
#include <algorithm>
#include <vector>

vector<std::string> split(std::string arg){
    int count = std::count(arg.begin(), arg.end(), ';');
    if (!count)
        return arg
    int pos = arg.find(";");
    while (pos != -1){

        pos = arg.find(";");
    }
}
std::optional<std::string> func(const std::string & arg){

}

int main(){

}